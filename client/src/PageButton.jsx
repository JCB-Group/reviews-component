import React from 'react';

//each of these divs will represent a number (a page number which will reference 7 review parents)
//each div will be passed down an onclick function that will set the current page in the main app to the button's number
//if passed no index number, render "..." by default, and have no onClick or onHover functionality